_d89‍.x([["default",()=>Constructos]]);class Constructos {
  _test(elements) {
    Object.keys(elements).forEach(key => {
      if (typeof elements[key] === "object") {
        // é mutable
      }
    });
  }

  _getIdentifier(elements) {
    return "";
  }

  /**
   * Div simples para inserção de texto
   * @param {object} elements
   * @param {string} elements.titulo Titulo da div
   * @param {string} elements.texto Texto a ser apresentado na simpleDiv
   */
  simpleDiv = elements => {
    this._test(elements);
    let identifier = this._getIdentifier(elements);
    return `
  <div id="[[simpleDiv]]" class="divGeral">
    <h1 id="[[h1TiTle]]">${elements.titulo}</h1>
    <p id="[[pTexto]]">
      ${elements.texto}
    </p>
  </div>
`;
  };

  /**
   * Span padrão para titulos de sessões
   * @param {object} elements
   * @param {string} elements.texto
   */
  spanTitle = elements => {
    this._test(elements);
    let identifier = this._getIdentifier(elements);
    return `
  <span class="spanTitle" id="[[spanTitle]]">
    ${elements.texto}
  </span>
`;
  };
}
